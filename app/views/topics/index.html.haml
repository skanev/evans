%h1 Форуми

%p= link_to 'Нова тема', new_topic_path

%table
  %thead
    %tr
      %th Тема
      %th Отговори
      %th Последен отговор
  %tbody
    - @topics.each do |topic|
      %tr
        %td= link_to topic.title, topic
        %td &mdash;
        %td
          %time{:date_time => topic.last_post_at}= topic.last_post_at

= will_paginate @topics
