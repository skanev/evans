%h1
  Материали
  = rss_feed lectures_url(:rss)

#lectures
  - unless @lectures.nil?
    %ol(start="0")
      - @lectures.each do |lecture|
        %li
          = link_to lecture.title, lecture.url
          (#{lecture.created_at})
  - else
    %p.missing Няма качени лекции
