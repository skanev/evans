#tips
  %h1 Хитринки
  - if admin?
    = link_to 'Създай нова', new_tip_path, class: :action
  - elsif logged_in?
    = link_to 'Предложи нова', new_tip_path, class: :action

  %table
    %thead
      %tr
        %th Заглавие
        %th Дата
    %tbody
      - @tips.each do |tip|
        %tr
          %td= link_to tip.title, tip
          %td
            - if tip.published_at?
              %time{:datetime => tip.published_at.iso8601}
                = relative_time_in_words tip.published_at
            - else
              Непубликувана
