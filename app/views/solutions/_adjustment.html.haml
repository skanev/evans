%h2 Корекция на точки
= simple_form_for [solution.task, solution] do |form|
  = form.input :adjustment, as: :radio_buttons, collection: -6.upto(3).map { |n| ["%+d" % n, n] }
  = form.button :submit, 'Промени'
